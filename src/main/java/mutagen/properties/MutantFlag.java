package mutagen.properties;

public enum MutantFlag
{
    COMPILABILITY,
    FUNCTIONALITY,
    STYLE,
    QUALITY,
    MUTAGEN_UNIQUE, // Unique to MutaGen
    USES_MAJOR,
    USES_AST,
    USES_STRING_MANIPULATION
}
